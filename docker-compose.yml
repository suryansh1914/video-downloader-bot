version: "3.8"

services:
  bot:
    build: .
    container_name: video-downloader-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot-data:/app/data

volumes:
  bot-data:
